package com.inventiapp.stocktrack.inventory.domain.model.queries;

/**
 * GetAllProvidersQuery
 * @summary
 * Query object representing a request to retrieve all Provider aggregates.
 * This simple query is intentionally parameterless; in the future it may be extended
 * with pagination, filtering or sorting parameters if required.
 * @since 1.0
 */
public record GetAllProvidersQuery() {}